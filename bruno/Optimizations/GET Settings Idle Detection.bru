meta {
  name: GET Settings Idle Detection
  type: http
  seq: 22
}

get {
  url: {{baseURL}}/recommendations/openshift/settings/idle-detection
  body: none
  auth: inherit
}

headers {
  x-rh-identity: {{xRhIdentity}}
}

settings {
  encodeUrl: true
  timeout: 0
}
