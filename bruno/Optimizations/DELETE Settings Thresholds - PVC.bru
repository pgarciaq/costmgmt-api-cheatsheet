meta {
  name: DELETE Settings Thresholds - PVC
  type: http
  seq: 30
}

delete {
  url: {{baseURL}}/recommendations/openshift/settings/thresholds?recommendation_type=pvc
  body: none
  auth: inherit
}

headers {
  x-rh-identity: {{xRhIdentity}}
}

settings {
  encodeUrl: true
  timeout: 0
}
