meta {
  name: Container recommendations - filter category zombie
  type: http
  seq: 64
}

get {
  url: {{baseURL}}/recommendations/openshift?filter[category]=zombie&order_by=last_reported&order_how=desc&limit=20&offset=0
  body: none
  auth: inherit
}

params:query {
  filter[category]: zombie
  order_by: last_reported
  order_how: desc
  limit: 20
  offset: 0
}

docs {
  filter[category]=zombie — abandoned workloads with zero CPU and memory across all digest rows.

  The API value is zombie; the UI may label these workloads as "Abandoned".
  Zombie rows expose estimated_monthly_waste (100% recoverable when cost data exists)
  and idle_recommendation guidance instead of rightsizing savings.

  Combine values with comma separation: filter[category]=idle,zombie

  Valid container/namespace categories: idle, zombie, undersized, oversized, optimized.
}

settings {
  encodeUrl: true
  timeout: 0
}
