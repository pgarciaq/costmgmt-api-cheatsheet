meta {
  name: Filter by idle state
  type: http
  seq: 6
}

get {
  url: {{baseURL}}/recommendations/openshift?filter[idle_state]=zombie,idle&order_by=last_reported&order_how=desc
  body: none
  auth: inherit
}

params:query {
  filter[idle_state]: zombie,idle
  order_by: last_reported
  order_how: desc
}

settings {
  encodeUrl: true
  timeout: 0
}
