meta {
  name: Filter by GPU
  type: http
  seq: 5
}

get {
  url: {{baseURL}}/recommendations/openshift?filter[has_gpu]=true&order_by=last_reported&order_how=desc
  body: none
  auth: inherit
}

params:query {
  filter[has_gpu]: true
  order_by: last_reported
  order_how: desc
}

settings {
  encodeUrl: true
  timeout: 0
}
