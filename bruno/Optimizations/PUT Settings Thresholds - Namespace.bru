meta {
  name: PUT Settings Thresholds - Namespace
  type: http
  seq: 71
}

put {
  url: {{baseURL}}/recommendations/openshift/settings/namespace
  body: json
  auth: inherit
}

headers {
  x-rh-identity: {{xRhIdentity}}
  content-type: application/json
}

body:json {
  {
    "cpu_cost_percentile": 0.65,
    "sparse_data_threshold": 2
  }
}

docs {
  PUT namespace threshold overrides. `sparse_data_threshold` (1–30, default 2) controls when
  notification code 77 (`SPARSE_DATA`) is emitted based on absolute `data_days`.
}

settings {
  encodeUrl: true
  timeout: 0
}
