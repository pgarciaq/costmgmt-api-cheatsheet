meta {
  name: VM term settings
  type: http
  seq: 14
}

get {
  url: {{baseURL}}/recommendations/openshift/settings/vm/terms
  body: none
  auth: inherit
}

headers {
  x-rh-identity: {{xRhIdentity}}
}

docs {
  Short/medium/long term windows for VM recommendations only (separate from /settings/terms).
}

settings {
  encodeUrl: true
  timeout: 0
}

assert {
  res.status: eq 200
}
