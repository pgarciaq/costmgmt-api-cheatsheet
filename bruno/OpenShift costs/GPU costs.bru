meta {
  name: GPU costs
  type: http
  seq: 16
}

get {
  url: {{baseURL}}/reports/openshift/gpu/?filter[time_scope_units]=month&filter[time_scope_value]=-1&filter[resolution]=monthly
  body: none
  auth: inherit
}

params:query {
  filter[time_scope_units]: month
  filter[time_scope_value]: -1
  filter[resolution]: monthly
}

settings {
  encodeUrl: true
  timeout: 0
}
