meta {
  name: Tag costs (key="env")
  type: http
  seq: 6
}

get {
  url: {{baseURL}}/reports/openshift/costs/?filter[time_scope_units]=month&filter[time_scope_value]=-1&filter[resolution]=monthly&group_by[tag:env]=*
  body: none
  auth: inherit
}

params:query {
  filter[time_scope_units]: month
  filter[time_scope_value]: -1
  filter[resolution]: monthly
  "group_by[tag:env]": *
}

settings {
  encodeUrl: true
  timeout: 0
}
