meta {
  name: Per Google Cloud Project
  type: http
  seq: 4
}

get {
  url: {{baseURL}}/reports/gcp/costs/?currency=USD&delta=cost&filter[limit]=10&filter[offset]=0&filter[resolution]=monthly&filter[time_scope_units]=month&filter[time_scope_value]=-1&group_by[gcp_project]=*&order_by[cost]=desc
  body: none
  auth: inherit
}

params:query {
  currency: USD
  delta: cost
  filter[limit]: 10
  filter[offset]: 0
  filter[resolution]: monthly
  filter[time_scope_units]: month
  filter[time_scope_value]: -1
  group_by[gcp_project]: *
  order_by[cost]: desc
}

settings {
  encodeUrl: true
  timeout: 0
}
