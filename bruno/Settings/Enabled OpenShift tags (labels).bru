meta {
  name: Enabled OpenShift tags (labels)
  type: http
  seq: 4
}

get {
  url: {{baseURL}}/settings/tags/?filter[enabled]=true&filter[source_type]=OCP&limit=10&offset=0&order_by[key]=asc
  body: none
  auth: inherit
}

params:query {
  filter[enabled]: true
  filter[source_type]: OCP
  limit: 10
  offset: 0
  order_by[key]: asc
}

settings {
  encodeUrl: true
  timeout: 0
}
