meta {
  name: Per AWS EC2 VM
  type: http
  seq: 4
}

get {
  url: {{baseURL}}/reports/aws/resources/ec2-compute/?cost_type=calculated_amortized_cost&currency=USD&filter[resolution]=monthly&filter[time_scope_units]=month&filter[time_scope_value]=-1&limit=10&offset=0&order_by[cost]=desc
  body: none
  auth: inherit
}

params:query {
  cost_type: calculated_amortized_cost
  currency: USD
  filter[resolution]: monthly
  filter[time_scope_units]: month
  filter[time_scope_value]: -1
  limit: 10
  offset: 0
  order_by[cost]: desc
}

settings {
  encodeUrl: true
  timeout: 0
}
